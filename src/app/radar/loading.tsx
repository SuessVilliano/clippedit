import { PageSkeleton } from "@/components/Skeletons";

export default function Loading() {
  return (
    <PageSkeleton
      title="Clip Radar"
      subtitle="The best moments to clip right now — live breakouts to capture and clips already going viral to ride."
    />
  );
}
